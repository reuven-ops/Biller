// Courier registry. Phase 1 covers sources 1 through 13 and 20 (brief section 17).
// Phase 4 adds mac_sites, payer_policies, and the upload and import pipelines.
import type { Courier } from '../courier.js';

export const ALL_COURIERS: Courier[] = [];
