# Remit fixture

synthetic_remit.csv is entirely synthetic test data written for the Appendix B
importer tests. Every claim reference, amount, and code combination is invented;
no real remittance data appears here. The payer_id values reuse well-known
public payer identifiers only so the shape matches a real export.
